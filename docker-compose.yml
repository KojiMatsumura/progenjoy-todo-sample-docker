services:
  program-runner:
    build: .
    ports:
      - "${PROGRAM_RUNNER_PORT:-8787}:3000"
    environment:
      # api_id:1/2 は data/<programId>/data.json（DATA_DIR 直下）
      DATA_DIR: /app/data
      PORT: 3000
    volumes:
      - ./data:/app/data
